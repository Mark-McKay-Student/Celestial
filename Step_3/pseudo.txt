SET canvas
SET canvas width // 320x180 is true ratio, 4x for 720p, 6x for 1080p, 8x for 1440p
SET canvas height
SET tile size // 40x22.5 tiles of 8px, 32 if 4x, 48 if 6x, 64 for 8x

SET grid

class screen {

}

class tile {
  SET color // will be changed to tile texture if we get that far
  SET half tile // so we dont run into errors drawing the 40 half tiles at the bottom

  constructor(color) {
    SET this color (property) to color (argument)
  }
}

class player {
  SET x coordinate
  SET y coordinate
  constructor
}

class movingTile {

}

keyPressed() {

}

setup() {
  SET canvas to create new canvas // fancy p5js thing
}

draw() {
  make background light blue // fancy p5js thing

   IF keyIsDown(A) { // 65 for the letter A
    remove 1 from player x;
  }

  IF keyIsDown(W) { // 68 for the letter W
    add 1 to player x
  }

  IF keyIsDown(W) { // 87 for the letter W
    remove 1 to player y
  }

  IF keyIsDown(S) { // 63 for the letter A
    add 1 to player y
  }
}
